<?xml version="1.0" encoding="UTF-8"?>
 <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.example.mapper.BookMapper"><!-- 인터페이스 mapper의 경로 -->

     <select id="getBookList" resultType="com.example.model.Book">
     	SELECT * FROM "Book" 
     </select>
     <!-- 태그안에 resultMap을 사용하려면 <resultMap 태그를 설정해줘야 오류가 발생하지않음 --> 
     <select id="getBook" resultType="com.example.model.Book">
     SELECT * FROM "Book" WHERE Id = #{Id}
     </select>
     <select id="getBookOne" resultType="com.example.model.Book">
     SELECT * FROM "Book" WHERE name LIKE %#{name}% or publisher Like %#{name}%;
     </select>
	<insert id="insertBook">
		INSERT INTO "Book"(name, publisher, "updateDate") VALUES (#{name}, #{publisher}, #{updateDate});
		<!-- #{ } 안의 값은 BookVO의 변수값이다 --> 
	</insert>
	<update id="updateBook">
		UPDATE "Book"
			SET name = #{name}, publisher = #{publisher}
				WHERE Id = #{Id}
	</update>
	<delete id="deleteBook">
		DELETE FROM "Book"
	</delete>
	
	<delete id="deleteBookOne">
		DELETE FROM "Book" WHERE Id = #{Id}
	</delete>
 </mapper>

 
 
 